import { View, Text } from 'react-native';
import { MainContaier } from '../components/MainContainer';

export default function ScreenFavorite() {
    return (
        <MainContaier>
            <Text>ScreenFavorite</Text>
        </MainContaier>
    );
}