import { View, Text } from 'react-native';
import { MainContaier } from '../components/MainContainer';

export default function ScreenOrder() {
    return (
        <MainContaier>
            <Text>ScreenOrder</Text>
        </MainContaier>
    );
}